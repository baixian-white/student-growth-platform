# 🎓 学生成长平台 - 环境配置指南

本指南旨在帮助开发者在不同机器上快速搭建开发环境，确保前后端及数据库的连接配置正确。

## 🛠️ 1. 环境准备

| 组件 | 版本要求 | 用途 |
| :--- | :--- | :--- |
| **JDK** | 17+ | 后端服务运行环境 |
| **Node.js** | 18+ (推荐 20) | 前端构建与开发服务器 |
| **PostgreSQL** | 12+ | 数据持久化存储 |
| **Maven** | 3.6+ | 后端依赖管理与构建 |

---

## 🗄️ 2. 数据库配置 (PostgreSQL)

### 2.1 初始化数据库
请在 PostgreSQL 控制台或 GUI 工具（如 pgAdmin）中执行以下脚本：

```sql
-- 1. 创建数据库
CREATE DATABASE education_db;

-- 2. 创建用户 (如果需要独立用户)
CREATE USER education_user WITH PASSWORD 'education_pass';

-- 3. 授权
GRANT ALL PRIVILEGES ON DATABASE education_db TO education_user;
```

> [!NOTE]
> 后端配置了 `spring.jpa.hibernate.ddl-auto: update`，因此表结构会在首次启动时自动生成，无需手动运行建表语句。

### 2.2 连接配置
后端配置文件路径：`backend/src/main/resources/application.yml`

```yaml
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/education_db
    username: education_user
    password: education_pass
```

---

## ⚙️ 3. 后端配置 (Spring Boot)

*   **默认端口**：`8080`
*   **上下文路径**：`/api` (即所有接口需访问 `http://localhost:8080/api/...`)
*   **API 文档**：
    *   Swagger UI: [http://localhost:8080/api/swagger-ui.html](http://localhost:8080/api/swagger-ui.html)
    *   API Docs: [http://localhost:8080/api/api-docs](http://localhost:8080/api/api-docs)

---

## 🌐 4. 前端与后端连接配置 (Vite)

项目使用 **Vite 代理模式** 解决跨域问题。

### 4.1 代理配置
配置文件路径：`frontend/vite.config.js`

```javascript
server: {
  port: 5173,  // 前端访问端口
  proxy: {
    '/api': {
      target: 'http://localhost:8080', // 指向后端服务地址
      changeOrigin: true,
      secure: false
    }
  }
}
```

### 4.2 验证连接
1.  启动后端：`mvn spring-boot:run`
2.  启动前端：`npm run dev`
3.  访问前端地址并观察是否有数据加载。如果后端端口变更，请务必同步修改 `vite.config.js` 中的 `target` 值。

---

## 🚀 5. 环境迁移 Checklist

如果你是在全新的机器上克隆本项目，请按此顺序操作：
1.  [ ] **数据库**：先安装 PostgreSQL 并根据 2.1 节初始化库。
2.  [ ] **后端依赖**：在 `backend` 目录下运行 `mvn install`。
3.  [ ] **前端依赖**：在 `frontend` 目录下运行 `npm install`。
4.  [ ] **启动顺序**：先启动后端，确认日志中没有数据库连接报错后，再启动前端。

---

## ❓ 常见问题
*   **连接超时**：请检查防火墙是否允许 5432 (DB) 和 8080 (Backend) 端口。
*   **404 Not Found**：请检查后端上下文路径是否包含 `/api`。
